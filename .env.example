# The Odds API key (local dev only — not used in production)
# Sign up at https://the-odds-api.com (free tier: 500 requests/month)
VITE_ODDS_API_KEY=your_key_here

# Cloudflare Worker proxy URL (production) — key never exposed in client code
# VITE_ODDS_PROXY_URL=https://odds-proxy.your-subdomain.workers.dev
